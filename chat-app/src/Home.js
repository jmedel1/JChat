import React from 'react';

const Home = () => {
  const headingStyle = {
    color: 'blue',
    fontSize: '24px',
    // Add any other desired inline styles
  };

  return <h2 style={headingStyle}>Home</h2>;
};

export default Home;
